import { PaginationQueryDto } from '../../../common/dto/pagination-query.dto';

/** Activity feed pagination (keyset, newest first). */
export class ListActivityQueryDto extends PaginationQueryDto {}
